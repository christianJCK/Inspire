{% extends 'base.html.twig' %}

{% block body %}
    <h1>Phrase</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phrase.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ phrase.name }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ phrase.comment }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('phrase_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('phrase_edit', { 'id': phrase.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
